/**
 * Brand assets Ibix Market — fonte canônica é a vitrine web.
 * Estes arquivos são CÓPIA BIT-A-BIT de `app/static/img/ibix/*` e
 * `app/static/img/landing/logoSfundo.png`. Quando a vitrine atualizar
 * o logo, copie novamente — NUNCA recrie do zero.
 *
 * Documentação: `mobile_marketplace/AGENTS.md` § 6 e regra § 8.1.
 */

export const brand = {
  /** Logo do header da vitrine (1412×436). Use no `BrandLogo` (header in-app). */
  cabPng: require('./cab.png'),
  /** Variante para fundos escuros / dark mode (833×336). */
  rodapePng: require('./rodape.png'),
  /** Logo institucional quadrado (1536×1024) — fonte do ícone do app. */
  logoSfundo: require('./logoSfundo.png'),
};
